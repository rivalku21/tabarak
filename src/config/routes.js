import { EntryPage, SavePage } from "../pages";

const routes = [
    {
        path: '/',
        component: EntryPage,
    },
    {
        path: '/event',
        component: SavePage,
    },
];

export default routes;